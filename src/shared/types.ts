export type ChildrenProps = {
  children: React.ReactNode;
  className?: string;
};

export type Experience = {
  number: number;
  title: string;
};

export type Partners = {
  image: string;
  title?: string;
};
